<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Tracking - Ma Locozz Clothing Brand</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background-color: #f5f5f5;
            padding: 20px;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
            overflow: hidden;
        }

        .header {
            background: linear-gradient(135deg, #3b7ea1 0%, #2c5f7f 100%);
            color: white;
            padding: 40px 30px;
            text-align: center;
        }

        .tracking-icon {
            width: 80px;
            height: 80px;
            background: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            font-size: 48px;
        }

        .header h1 {
            font-size: 28px;
            margin-bottom: 10px;
        }

        .header p {
            font-size: 16px;
            opacity: 0.9;
        }

        .content {
            padding: 30px;
        }

        .order-info {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 6px;
            margin-bottom: 30px;
        }

        .order-info h2 {
            color: #2c5f7f;
            font-size: 20px;
            margin-bottom: 15px;
        }

        .info-row {
            display: flex;
            justify-content: space-between;
            padding: 10px 0;
            border-bottom: 1px solid #e0e0e0;
        }

        .info-row:last-child {
            border-bottom: none;
        }

        .info-label {
            font-weight: 600;
            color: #555;
        }

        .info-value {
            color: #333;
        }

        .tracking-section {
            margin-bottom: 30px;
        }

        .tracking-section h2 {
            color: #2c5f7f;
            font-size: 20px;
            margin-bottom: 20px;
        }

        .tracking-timeline {
            position: relative;
            padding-left: 30px;
        }

        .tracking-timeline::before {
            content: '';
            position: absolute;
            left: 15px;
            top: 0;
            bottom: 0;
            width: 2px;
            background: #e0e0e0;
        }

        .tracking-step {
            position: relative;
            margin-bottom: 20px;
            padding-left: 30px;
        }

        .tracking-step::before {
            content: '';
            position: absolute;
            left: -15px;
            top: 5px;
            width: 10px;
            height: 10px;
            border-radius: 50%;
            background: #e0e0e0;
        }

        .tracking-step.completed::before {
            background: #2c5f7f;
        }

        .tracking-step.active::before {
            background: #3b7ea1;
            box-shadow: 0 0 0 3px rgba(59, 126, 161, 0.3);
        }

        .step-title {
            font-weight: 600;
            color: #333;
            margin-bottom: 5px;
        }

        .step-date {
            font-size: 14px;
            color: #666;
        }

        .step-description {
            font-size: 14px;
            color: #666;
            margin-top: 5px;
        }

        .items-section {
            margin-bottom: 30px;
        }

        .items-section h2 {
            color: #2c5f7f;
            font-size: 20px;
            margin-bottom: 20px;
        }

        .item {
            display: flex;
            gap: 20px;
            padding: 15px;
            border: 1px solid #e0e0e0;
            border-radius: 6px;
            margin-bottom: 15px;
        }

        .item-image {
            width: 80px;
            height: 80px;
            background: #f0f0f0;
            border-radius: 4px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 40px;
        }

        .item-details {
            flex: 1;
        }

        .item-name {
            font-weight: 600;
            font-size: 16px;
            margin-bottom: 5px;
            color: #333;
        }

        .item-specs {
            font-size: 14px;
            color: #666;
            margin-bottom: 5px;
        }

        .item-price {
            font-size: 16px;
            color: #2c5f7f;
            font-weight: 600;
        }

        .summary {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 6px;
            margin-bottom: 30px;
        }

        .summary-row {
            display: flex;
            justify-content: space-between;
            padding: 10px 0;
            font-size: 16px;
        }

        .summary-row.total {
            border-top: 2px solid #2c5f7f;
            margin-top: 10px;
            padding-top: 15px;
            font-size: 20px;
            font-weight: 700;
            color: #2c5f7f;
        }

        .shipping-info {
            margin-bottom: 30px;
        }

        .shipping-info h2 {
            color: #2c5f7f;
            font-size: 20px;
            margin-bottom: 15px;
        }

        .address-box {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 6px;
            line-height: 1.6;
        }

        .actions {
            display: flex;
            gap: 15px;
            flex-wrap: wrap;
        }

        .btn {
            flex: 1;
            min-width: 200px;
            padding: 14px 28px;
            border: none;
            border-radius: 6px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            transition: all 0.3s ease;
        }

        .btn-primary {
            background: #2c5f7f;
            color: white;
        }

        .btn-primary:hover {
            background: #234d66;
        }

        .btn-secondary {
            background: white;
            color: #2c5f7f;
            border: 2px solid #2c5f7f;
        }

        .btn-secondary:hover {
            background: #f0f7fa;
        }

        .footer {
            text-align: center;
            padding: 30px;
            background: #f8f9fa;
            color: #666;
            font-size: 14px;
        }

        .footer p {
            margin-bottom: 10px;
        }

        @media (max-width: 600px) {
            .container {
                margin: 0;
            }

            .content {
                padding: 20px;
            }

            .item {
                flex-direction: column;
            }

            .actions {
                flex-direction: column;
            }

            .btn {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="tracking-icon">📦</div>
            <h1>Order Tracking</h1>
            <p>Track your order status</p>
        </div>

        <div class="content">
            <div class="order-info">
                <h2>Order Details</h2>
                <div class="info-row">
                    <span class="info-label">Order Number:</span>
                    <span class="info-value">#{{ order.order_number }}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Order Date:</span>
                    <span class="info-value">{{ order.created_at.strftime('%B %d, %Y') }}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Tracking Number:</span>
                    <span class="info-value">{{ order.order_number }}</span>  <!-- Placeholder; add a real tracking field if needed -->
                </div>
                <div class="info-row">
                    <span class="info-label">Estimated Delivery:</span>
                    <span class="info-value">3-5 business days</span>  <!-- Placeholder -->
                </div>
            </div>

            <div class="tracking-section">
                <h2>Order Status</h2>
                <div class="tracking-timeline">
                    {% for step in timeline %}
                    <div class="tracking-step {{ 'completed' if step.completed else 'active' if loop.last and not step.completed else '' }}">
                        <div class="step-title">{{ step.status }}</div>
                        <div class="step-date">{{ step.date.strftime('%B %d, %Y') if step.date else 'Pending' }}</div>
                        <div class="step-description">
                            {% if step.status == 'Order Placed' %}
                                Your order has been received and is being processed.
                            {% elif step.status == 'Processing' %}
                                Your items are being prepared for shipment.
                            {% elif step.status == 'Shipped' %}
                                Your order has been shipped.
                            {% elif step.status == 'Delivered' %}
                                Your order has been delivered successfully.
                            {% endif %}
                        </div>
                    </div>
                    {% endfor %}
                </div>
            </div>

            <div class="items-section">
                <h2>Order Items</h2>
                {% for item in order_items %}
                <div class="item">
                    <div class="item-image">👕</div>
                    <div class="item-details">
                        <div class="item-name">{{ item.product_name }}</div>
                        <div class="item-specs">Size: {{ item.size or 'N/A' }} | Color: {{ item.color or 'N/A' }}</div>
                        <div class="item-specs">Quantity: {{ item.quantity }}</div>
                        <div class="item-price">₱{{ "%.2f"|format(item.price * item.quantity) }}</div>
                    </div>
                </div>
                {% endfor %}
            </div>

            <div class="summary">
                <div class="summary-row">
                    <span>Subtotal:</span>
                    <span>₱{{ "%.2f"|format(order.subtotal) }}</span>
                </div>
                <div class="summary-row">
                    <span>Shipping:</span>
                    <span>₱{{ "%.2f"|format(order.shipping_cost) }}</span>
                </div>
                <div class="summary-row">
                    <span>Tax:</span>
                    <span>₱{{ "%.2f"|format(order.tax) }}</span>
                </div>
                <div class="summary-row total">
                    <span>Total:</span>
                    <span>₱{{ "%.2f"|format(order.total) }}</span>
                </div>
            </div>

            <div class="shipping-info">
                <h2>Shipping Address</h2>
                <div class="address-box">
                    <strong>{{ order.user.first_name }} {{ order.user.last_name }}</strong><br>
                    {{ order.shipping_address_line1 }}<br>
                    {% if order.shipping_address_line2 %}{{ order.shipping_address_line2 }}<br>{% endif %}
                    {{ order.shipping_city }}, {{ order.shipping_state }} {{ order.shipping_postal_code }}<br>
                    {{ order.shipping_country }}<br>
                    Phone: {{ order.shipping_phone }}
                </div>
            </div>

            <div class="actions">
                <a href="{{ url_for('orders_list') }}" class="btn btn-primary">View All Orders</a>
                <a href="/" class="btn btn-secondary">Continue Shopping</a>
            </div>
        </div>

        <div class="footer">
            <p>Questions? Contact us at support@malocozz.com</p>
            <p>&copy; 2025 Ma Locozz Clothing Brand. All rights reserved.</p>
        </div>
    </div>
</body>
</html>
